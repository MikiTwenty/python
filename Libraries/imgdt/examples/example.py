from imgdt import run

run(
    dataset_folder = "path\\to\\folder",
    target_folder = "path\\to\\folder",
    history_file = "path\\to\\file",
    dataset_name = 'test',
    scaling_size = 1080,
    remove_originals = False
)